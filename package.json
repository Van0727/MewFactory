{
  "name": "mew",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen:assets": "node scripts/genPlaceholders.mjs",
    "gen:tiles": "node scripts/genPlaceholders.mjs --tiles-only"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
